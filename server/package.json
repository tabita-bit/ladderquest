{
  "name": "ladderquest-server",
  "version": "1.0.0",
  "description": "BFS-powered word ladder solver — backend API + static frontend host",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
